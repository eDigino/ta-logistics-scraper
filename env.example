# MongoDB Configuration
# Get your connection string from MongoDB Atlas:
# 1. Go to https://cloud.mongodb.com
# 2. Navigate to Database → Connect → Connect your application
# 3. Copy the connection string and replace the values below

# Replace the entire URI with your MongoDB Atlas connection string
# Format: mongodb+srv://username:password@cluster.mongodb.net/?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://your_username:your_password@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority

# Database name (you can keep these defaults or change them)
DATABASE_NAME=copart_scraper

# Collection name for storing vehicles
COLLECTION_NAME=vehicles

# IMPORTANT: 
# - Replace 'your_username' with your MongoDB Atlas username
# - Replace 'your_password' with your MongoDB Atlas password
# - Replace 'cluster0.xxxxx' with your actual cluster address
# - If your password contains special characters, they need to be URL encoded:
#   @ → %40, : → %3A, / → %2F, ? → %3F, # → %23, [ → %5B, ] → %5D, % → %25

